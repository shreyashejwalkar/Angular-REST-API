// defining a custom type using a TypeScript interface with the required properties.

export interface Product
{
    id: number;
    name: string;
    description: string;
    price: number;
    quantity: number;
    imageUrl: string;
}